apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: network-nginx
  namespace: default

spec:
  podSelector:
    matchLabels:
      app: nginx

  policyTypes:
    - Ingress
    - Egress

  ingress:
    - ports:
        - port: 80
